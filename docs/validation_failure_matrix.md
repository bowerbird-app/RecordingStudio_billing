# Integration Validation Failure Matrix

| Command | Failure | Root cause | Minimal fix | Final result |
| --- | --- | --- | --- | --- |
| `bundle exec ruby -Itest test/admin_operations_test.rb` | Recording Studio child records disappeared or deadlocked during fixture setup when cleanup ran concurrently. | The standalone test did not hold the cleanup helper's advisory lock or clean its fixtures after each test. | Hold `BillingTestDatabaseCleanup::LOCK_NAMESPACE`, clear before fixture creation and during teardown, and release in `ensure`. | Passed: 18 runs, 110 assertions, 0 failures, 0 errors. |
| `bundle exec ruby -Itest test/dummy/test/integration/billing_journeys_test.rb` | The dummy seed used caller-supplied overage terms and lifecycle code lacked `SubscriptionItemVersion` Recording Studio associations. | Overage authority must come from the published manifest, and lifecycle lookups require authoritative recording associations. | Rate against the published manifest containing the seeded Meter and OveragePrice; call `calculate_overage(allocation:)`; add the version recording associations. | Passed with the dummy app bundle: 12 runs, 59 assertions, 0 failures, 0 errors. The journey's idempotency test loads seeds a second time. |
| `bundle exec rake test` | Not rerun in this focused slice. | Other concurrent worktree changes and the full-gate validation are outside this slice. | Run once all slices settle. | Not yet run against the final combined worktree. |
| Stripe test credentials and host configuration | Real Stripe calls require host-owned restricted keys, a pinned Stripe API version, allowed return URLs, and Stripe Tax registrations when tax is on. | The dummy test environment supplies no Stripe credential resolver. Development reads Cursor `stripe_test_*` secrets or `STRIPE_TEST_*` aliases when they are `sk_test` / `rk_test` / `pk_test`. | Configure `stripe_credential_resolver` in the host with `rk_` keys. Pin Stripe-Version `2026-07-29.dahlia`. In dummy development, set those test keys and run `bin/rails stripe:ping`. Enable tax only after Dashboard Tax registrations exist. | Dummy tests stay fail-closed. The live probe runs only when test keys are present. |