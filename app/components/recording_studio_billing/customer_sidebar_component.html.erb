<%= render FlatPack::Sidebar::Group::Component.new(
  label: "Billing",
  icon: :credit_card,
  default_open: billing_open?
) do |group| %>
  <% group.items do %>
    <% items.each do |item| %>
      <%= render FlatPack::Sidebar::Item::Component.new(
        label: item.fetch(:label),
        href: item.fetch(:href),
        icon: item[:icon],
        active: item[:page]&.to_sym == current_page
      ) %>
    <% end %>
  <% end %>
<% end %>
