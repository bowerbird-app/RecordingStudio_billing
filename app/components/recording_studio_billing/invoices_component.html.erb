<%= render FlatPack::PageTitle::Component.new(title: @presenter.copy("invoices_title", "Invoices"), variant: :h1) %>
<%= render RecordingStudioBilling::BillingNavigationComponent.new(presenter: @presenter, current_page: :invoices) %>
<% header_extensions.each { |extension| %><%= render_extension(extension) %><% } %>
<%= render FlatPack::List::Component.new do %>
  <% @presenter.invoices.each do |invoice| %><li><%= link_to invoice.id, helpers.invoice_path(invoice, root_recording_id: @presenter.root_recording.id) %>: <%= @presenter.invoice_status(invoice) %> - <%= @presenter.invoice_total(invoice) %></li><% end %>
<% end %>
<% if @presenter.adjustments.present? %>
  <%= render FlatPack::List::Component.new do %>
    <% @presenter.adjustments.each do |adjustment| %><li><%= adjustment.kind.humanize %>: <%= adjustment.amount_minor %> <%= adjustment.currency_code %> (<%= adjustment.financial_command.state.humanize %>)</li><% end %>
  <% end %>
<% end %>
<% if @presenter.refunds.present? %>
  <%= render FlatPack::List::Component.new do %>
    <% @presenter.refunds.each do |refund| %><li>Refund: <%= refund.amount_minor %> <%= refund.currency_code %> (<%= refund.financial_command.state.humanize %>)</li><% end %>
  <% end %>
<% end %>
<% body_extensions.each { |extension| %><%= render_extension(extension) %><% } %>
<% @presenter.page_contents(:invoices).each { |content| %><%= render_extension(content) %><% } %>
<% footer_extensions.each { |extension| %><%= render_extension(extension) %><% } %>